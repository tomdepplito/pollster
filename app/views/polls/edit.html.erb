<h1>Edit Poll:</h1><br>

<%= form_for @poll do |p| %>
	<%= p.label :title %>
	<%= p.text_field :title %>
	
	<%= p.fields_for :questions do |q| %>
	<%= render 'shared/questions', :p => q %><br>
	<% end %>
	<%= p.submit "Create" %>
<% end %>
